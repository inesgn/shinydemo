### Shiny app demo: Running instructions
#
# set dir to the directory where both server.R and ui.R are hosted
#setwd()
# load shiny library
library(shiny)
# then simply runApp(). Display mode 'showcase' makes server.R and ui.R visible within the html page
#runApp(display.mode='showcase')
runApp()

